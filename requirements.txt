# TF-DFE: Core dependencies
# Install with: pip install -r requirements.txt
# Python 3.8+ required

#Data processing
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0

#Machine learning
xgboost>=1.7.0
lightgbm>=3.3.0
catboost>=1.1.0

#Topological Data Analysis 
giotto-tda>=0.6.0

#Structural biology 
biopython>=1.79          # PDB parsing, SASA computation (ShrakeRupley)
pyfaidx>=0.7.0           # FASTA sequence access for FCGR feature extraction

#Interpretability
shap>=0.41.0

#Visualization 
matplotlib>=3.6.0
seaborn>=0.12.0

#Utilities 
tqdm>=4.64.0             # Progress bars
joblib>=1.2.0            # Parallel processing

#Optional: GPU acceleration (uncomment if NVIDIA GPU available)
# cudf-cu11>=23.0.0      # GPU-accelerated pandas (RAPIDS) — Script 04 only
